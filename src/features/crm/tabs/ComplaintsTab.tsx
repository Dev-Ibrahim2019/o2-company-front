import { DomainTable, SectionFrame, StatusChip, date, text, unwrapRows, useCrmSection } from "./shared";
export default function ComplaintsTab(){const state=useCrmSection("complaints");return <SectionFrame state={state}>{d=><DomainTable empty="لا توجد شكاوى مسجلة" rows={unwrapRows(d,["complaints"])} columns={[{key:"number",label:"رقم الشكوى",render:(v,r)=>text(v??r.code??r.id)},{key:"subject",label:"الموضوع",render:(v,r)=>text(v??r.title)},{key:"status",label:"الحالة",render:v=><StatusChip value={text(v)}/>},{key:"priority",label:"الأولوية"},{key:"created_at",label:"تاريخ التسجيل",render:date}]}/>}</SectionFrame>}
